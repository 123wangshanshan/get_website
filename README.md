# Download Link Scraper

这是一个用于抓取网页下载链接并保存到文本文件的脚本。

示例用法：

```bash
python get_download_links.py --url https://conda.anaconda.org/bioconda/linux-64/ --output download_links.txt
```

以上命令会将提取到的下载链接写入当前目录下的 `download_links.txt` 文件。
